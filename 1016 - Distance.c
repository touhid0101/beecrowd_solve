// 1016 - Distance - Beecrowd
#include <stdio.h>

int main() {
    // taking distance as input
    int distance;
    scanf("%d", &distance);

    // calculating time
    int time = distance * 2; // Y gets away from X 2km/min

    // printing the output
    printf("%d minutos\n", time);

    return 0;
}
