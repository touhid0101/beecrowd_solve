// 1929 - Triangle - Beecrowd
#include <stdbool.h>
#include <stdio.h>

int main() {
    // taking length of 4 rods as input
    int rod[4];
    scanf("%d %d %d %d", &rod[0], &rod[1], &rod[2], &rod[3]);

    // sorting the rods in chronological order
    for (int i = 0, swap_count = 1; i < 4; i++) {
        if (swap_count != 1) {
            for (int j = 0; j < 3; j++) {
            }
        } else {
            break;
        }
    }

    if () {
        printf("S\n");
    } else {
        printf("N\n");
    }

    return 0;
}