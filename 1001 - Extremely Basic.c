// 1001 - Extremely Basic - Beecrowd
#include <stdio.h>

int main() {
    // declaring variables
    int num1, num2, sum;

    // taking inputs
    scanf("%d", &num1);
    scanf("%d", &num2);

    // calculating the sum of the numbers
    sum = num1 + num2;

    // printing the sum of the numbers
    printf("X = %d\n", sum);

    return 0;
}